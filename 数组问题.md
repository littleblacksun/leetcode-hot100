在普通数组问题中，都是用到了类似于前缀和的一种思想

区间子数组的和可以看做成为两个子数组的差

那么只需要用一个数组统计数组的前缀和：

```bash
ans[0] = nums[0]
ans[1] = nums[0]+nums[1] = ans[0] + nums[1]
ans[2] = nums[0]+nums[1]+nums[2] = ans[1] + nums[2]
...
```

得到一段数组的区间和=ans[k] - ans[i] 即可
