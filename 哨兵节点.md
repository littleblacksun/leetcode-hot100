# 用最简单、最直观的例子，彻底搞懂「哨兵节点（dummy node）」
一句话先讲透：
**哨兵节点 = 一个假的头节点，放在链表最前面，专门帮你解决“删除/插入第一个节点”时的麻烦。**

下面我用**对比例子**，让你一眼看懂为什么需要它。

---

# 1. 先看没有哨兵的麻烦
假设链表：`1 → 2 → 3`
我们要**删除第一个节点 1**。

没有哨兵时：
- 原来的头是 `1`
- 删除后头变成 `2`
- 你必须**单独判断**：如果删的是头节点，要 `return head.next`

代码逻辑要分两种：
1. 删除中间节点
2. 删除头节点

很麻烦，容易写错。

---

# 2. 有哨兵之后，一切统一
哨兵结构：
`dummy → 1 → 2 → 3`

现在**不管删哪个节点**，逻辑永远一样：
- 找到**待删节点的前一个节点 prev**
- `prev.next = prev.next.next`

哪怕删的是第一个节点 `1`：
- prev 就是 dummy
- `dummy.next = dummy.next.next`
直接搞定！

**不需要任何特殊判断！**

---

# 3. 用 LeetCode19 真实例子看
## 例子1：正常删除中间节点
链表：`1 → 2 → 3 → 4 → 5`，删除倒数第2个（4）

哨兵结构：
`dummy →1→2→3→4→5`

最终 left 指向 3
`left.next = 5`
成功删除 4

---

## 例子2：删除头节点（最能体现哨兵价值）
链表：`1 → 2 → 3`，删除倒数第3个（就是1）

哨兵结构：
`dummy →1→2→3`

快指针走3步，最后 left 停在 dummy
执行：
`dummy.next = dummy.next.next`

链表变成：
`dummy → 2 →3`

返回 `dummy.next` 就是新头 `2`
**完全不用判断是不是头节点！**

---

## 例子3：链表只有一个节点（经典边界）
链表：`1`，删除倒数第1个

哨兵结构：
`dummy →1`

left 最终 = dummy
执行：
`dummy.next = null`

返回 `dummy.next = null`
正确！

如果**没有哨兵**，你要单独写：
```java
if(head.next == null) return null;
```
有哨兵，**一句都不用多写**。

---

# 4. 超级总结（记住这三句就行）
1. **哨兵节点 = 虚拟头节点，永远在链表最前面**
2. **它的作用：统一所有节点的增删逻辑，不用单独处理头节点**
3. **最后返回 dummy.next，就是真实链表头**

---

# 5. 一句话记忆
**没有哨兵，删头要特判；
有哨兵，所有节点删法一模一样。**

如果你愿意，我可以画一张**ASCII图解**，让你一眼看懂哨兵在快慢指针里到底怎么工作的。
