<img width="809" height="808" alt="image" src="https://github.com/user-attachments/assets/7e984fb5-4cdd-42b0-875e-152f15d48dee" />

# 用大白话讲懂：二叉树展开为链表（后序递归版）
我先把你代码**修正**（你写了`root.left == null`是判断，应该改成赋值`root.left = null`），再一步一步讲透，保证你能看懂！

先给你**完整正确代码**：
```java
class Solution {
    // 全局变量：记录当前链表的最后一个节点
    private TreeNode head = null;
    
    public void flatten(TreeNode root) {
        if (root == null) {
            return;
        }
        // 1. 先递归处理右子树
        flatten(root.right);
        // 2. 再递归处理左子树
        flatten(root.left);
        
        // 核心：拼接链表
        root.left = null;   // 左子树置空
        root.right = head;  // 当前节点的右指针 指向 已拼接好的链表头
        head = root;        // 更新链表头为 当前节点
    }
}
```

---

## 一、先搞懂题目要求
把**二叉树** 变成 **只有右孩子的单链表**，顺序是**前序遍历**：
`根节点 → 左子树 → 右子树`

举个最简单的例子：
```
    1        展开后变成：
   / \        1
  2   3         \
                  2
                   \
                    3
```

---

## 二、核心思路：**倒着拼接链表**（超级关键！）
正常前序是：根→左→右
但这个代码是**反过来**：**先右 → 再左 → 最后根**（后序遍历）
目的：**从链表的最后一个节点开始，往前拼接**

我们用一个变量 `head`：
- 永远保存**已经拼好的链表的第一个节点**
- 新节点直接接在 `head` 前面

---

## 三、手把手举例子（拿最简单的树演示）
我们用这棵树演示：
```
    1
   / \
  2   3
```

### 第一步：递归处理 右子树（节点3）
1. 节点3没有右孩子 → `flatten(null)` 直接返回
2. 节点3没有左孩子 → `flatten(null)` 直接返回
3. 拼接：
   - `3.left = null`
   - `3.right = head`（此时head是null）
   - `head = 3`
✅ 现在链表：`3`，`head=3`

---

### 第二步：递归处理 左子树（节点2）
1. 节点2没有右孩子 → 返回
2. 节点2没有左孩子 → 返回
3. 拼接：
   - `2.left = null`
   - `2.right = head`（head=3）
   - `head = 2`
✅ 现在链表：`2 → 3`，`head=2`

---

### 第三步：处理 根节点（节点1）
1. 右子树、左子树都处理完了
2. 拼接：
   - `1.left = null`
   - `1.right = head`（head=2）
   - `head = 1`
✅ 最终链表：`1 → 2 → 3`

完美符合题目要求！

---

## 四、代码每一行到底在干嘛？
```java
// 空节点直接返回
if(root == null) return;

// 先处理右子树：因为我们要倒着拼，右子树在链表最后面
flatten(root.right);

// 再处理左子树：左子树在链表中间
flatten(root.left);

// 左子树必须清空（题目要求）
root.left = null;

// 当前节点的右指针 → 已经拼好的链表（head指向它）
root.right = head;

// 更新head：把当前节点变成新的链表头
head = root;
```

---

## 五、一句话总结这个解法的逻辑
**从链表的最后一个节点开始，倒着往前拼接：先处理最右边的节点，再处理左边，最后处理根，每一步都把当前节点接在已拼好的链表前面。**

---

### 总结
1. 这是**后序递归**：右→左→根
2. `head` 是**已拼接好的链表头**
3. 核心操作：`当前节点.right = head`，再更新`head=当前节点`
4. 最终得到**前序顺序的右斜链表**

这个思路是不是一下子就清晰了？

# LeetCode114 二叉树展开链表 · 分治递归版 超通俗讲解
这个**分治思路**比上一个倒序后序好理解一万倍，逻辑完全贴合二叉树本身结构，我用大白话+画图一步步带你看懂。

## 先记住题目要求
把二叉树拉直成**只有右孩子的单链表**
顺序：**根 → 左子树全部 → 右子树全部**

## 这个函数 dfs 到底干嘛？
```java
private TreeNode dfs(TreeNode root)
```
**作用：把以 root 为根的整棵子树铺平成链表，返回这棵链表的【最后一个尾巴节点】**

就一句话：
> 进来一棵子树，我帮你捋直，还给你这串链表**末尾是谁**

---

# 核心分治逻辑（超级好记）
1. 先把**左子树**铺平，拿到左子树链表尾巴 `leftTail`
2. 再把**右子树**铺平，拿到右子树链表尾巴 `rightTail`
3. 如果有左子树：
   - 左尾巴的右指针 → 接上原来的右子树
   - 根的右指针 → 接上左子树
   - 根的左指针置空
4. 返回当前整棵树铺平后的**最后尾巴**

---

# 举一模一样的例子
```
    1
   / \
  2   3
```

## 1、递归处理左子树 节点2
- 2 左、右都是空
- `leftTail=null`，`rightTail=null`
- 没有左子树，不用拼接
- dfs(2) 返回 **2**（自己就是尾巴）

## 2、递归处理右子树 节点3
- 3 左右都空
- dfs(3) 返回 **3**

## 3、回到根节点 1 开始拼接
```java
leftTail = 2
rightTail = 3
```
因为 `leftTail != null`，执行三步拼接：
1. `2.right = 1.right` → **2 接上 3**
2. `1.right = 1.left` → **1 接上 2**
3. `1.left = null`

此时树变成：
```
1
 \
  2
   \
    3
```

最后返回尾巴：`rightTail=3`
整棵树处理完毕！

---

# 再看最难懂的return语句
```java
return rightTail != null ? rightTail : leftTail != null ? leftTail : root ;
```
三目运算符，翻译成人话：
1. **右子树有尾巴** → 整个树尾巴就是右尾巴
2. 右没有，**左子树有尾巴** → 尾巴就是左尾巴
3. 左右都没有 → 自己就是尾巴

优先级：
**右尾巴 > 左尾巴 > 根自己**

---

# 完整流程走一遍大树
```
      1
     / \
    2   5
   / \   \
  3   4   6
```
1. dfs(3) → 返回3
2. dfs(4) → 返回4
3. 处理2：
   3.right=4，2.right=3，左空 → 2链表尾巴是4
4. dfs(6) → 返回6
5. 处理5：右尾巴6，返回6
6. 处理1：
   左尾巴4 → 4.right=5
   1.right=2
   左置空
最终链表：
1→2→3→4→5→6

---

# 两句口诀背会这个分治写法
1. 递归找**左尾巴、右尾巴**
2. 左尾巴接右子树，根右接左子树，左清空
3. 谁在后谁当尾巴往上返回

---

# 对比两种写法一眼分清
1. **倒序后序（上一个）**：从后往前拼，用全局head
2. **分治递归（这个）**：从上往下分治，子树捋平→返回尾巴拼接，**面试最爱写、最好讲思路**

要不要我给你画**递归调用栈流程图**，你一眼就彻底悟透？

