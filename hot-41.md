# LeetCode 207. 课程表 题解笔记
## 题目描述
你这个学期必须选修 `numCourses` 门课程，记为 `0` 到 `numCourses - 1`。

在选修某些课程之前需要先修其他课程。先修课程用数组 `prerequisites` 表示，其中 `prerequisites[i] = [a, b]`，表示要学习课程 `a` 必须先学习课程 `b`。

请你判断是否可能完成所有课程的学习？

**示例：**
- 输入：`numCourses = 2`, `prerequisites = [[1,0]]`
- 输出：`true`
  - 解释：总共有 2 门课程。学习课程 1 之前，你需要完成课程 0。所以这是可能的。

- 输入：`numCourses = 2`, `prerequisites = [[1,0],[0,1]]`
- 输出：`false`
  - 解释：总共有 2 门课程。学习课程 1 之前，你需要先完成课程 0；学习课程 0 之前，你需要先完成课程 1。这是不可能的。

## 解题思路
核心解法：**深度优先搜索（DFS）检测有向图中的环**
1. **问题转化**：将课程与先修关系转化为**有向图**，课程是节点，先修关系是有向边。
2. **核心问题**：如果图中**存在环**（例如：A→B，B→A），则无法完成所有课程；反之则可以完成。
3. **三色标记法**：用三种颜色标记节点状态，高效检测环：
   - `0`：未被访问
   - `1`：正在访问中（当前递归栈内）
   - `2`：已访问完成
4. **DFS 遍历**：从每个未访问节点开始 DFS，若遍历过程中遇到**状态为 1** 的节点，说明找到环，返回 `false`。

## 完整代码（Java）
```java
class Solution {
    public boolean canFinish(int numCourses, int[][] prerequisites) {
        // 1. 构建邻接表表示的有向图
        List<Integer>[] g = new ArrayList[numCourses];
        // 初始化邻接表，每个节点对应一个空列表
        Arrays.setAll(g, i -> new ArrayList<>());
        // 遍历先修关系，添加有向边：p[1] -> p[0]
        // 含义：先学 p[1]，才能学 p[0]
        for(int[] p : prerequisites){
            g[p[1]].add(p[0]);
        }

        // 2. 三色标记数组
        // 0: 未访问  1: 正在访问  2: 已访问完成
        int[] colors = new int[numCourses];

        // 3. 遍历每个课程，进行DFS检测
        for(int i = 0; i < numCourses; i++){
            // 如果课程i未被访问，且DFS发现环，则直接返回false
            if(colors[i] == 0 && dfs(i, g, colors))
                return false;
        }
        // 所有课程检测完毕，无环，返回true
        return true;
    }

    /**
     * DFS 深度优先搜索
     * @param x 当前访问的课程节点
     * @param g 邻接表
     * @param colors 状态标记数组
     * @return true=找到环，false=无环
     */
    private boolean dfs(int x, List<Integer>[] g, int[] colors){
        // 标记当前节点为【正在访问中】
        colors[x] = 1;

        // 遍历当前节点的所有邻接节点（后续课程）
        for(int y : g[x]){
            // 情况1：邻接节点y正在访问中 → 找到环！
            // 情况2：邻接节点y未访问，递归DFS后发现环
            if(colors[y] == 1 || (colors[y] == 0 && dfs(y, g, colors))){
                return true; // 存在环，返回true
            }
        }

        // 所有邻接节点遍历完毕，标记当前节点为【已访问完成】
        colors[x] = 2;
        // 以x为起点的路径无环
        return false;
    }
}
```

## 代码解析
### 1. 构建有向图（邻接表）
- `List<Integer>[] g`：用数组+列表表示图，`g[i]` 存储课程 `i` 的所有后续课程。
- `Arrays.setAll(g, i -> new ArrayList<>())`：快速初始化邻接表，简洁高效。
- **边的方向**：`p[1] → p[0]`，严格对应**先修关系**。

### 2. 三色标记法
- **`0`（白色）**：节点从未被访问，是未探索的课程。
- **`1`（灰色）**：节点正在当前递归栈中，处于**探索过程中**。
- **`2`（黑色）**：节点及其所有子节点已探索完毕，无环。

### 3. DFS 核心逻辑
1. 访问节点 `x`，标记为**正在访问（1）**。
2. 遍历 `x` 的所有后继课程 `y`：
   - 若 `y` 是**正在访问（1）**：说明从 `y` 出发又走回了 `y`，**形成环**。
   - 若 `y` 是**未访问（0）**：递归访问 `y`，递归结果若为 `true`，说明子路径有环。
3. 所有后继节点遍历完成，标记 `x` 为**已访问完成（2）**。
4. 返回 `false`，表示当前路径**无环**。

### 4. 主函数逻辑
- 遍历每个课程，对未访问的课程启动 DFS。
- 任意一次 DFS 返回 `true`（找到环），立即返回 `false`（无法完成课程）。
- 全部遍历完成未找到环，返回 `true`（可以完成）。

## 复杂度分析
- **时间复杂度**：$O(N + E)$
  - $N$ 为课程数（节点数），$E$ 为先修关系数（边数）。
  - 每个节点、每条边仅访问一次。
- **空间复杂度**：$O(N + E)$
  - 邻接表存储所有边，占用 $O(E)$。
  - 递归栈深度最坏为 $O(N)$（链表状图），颜色数组 $O(N)$。

## 关键点总结
1. **问题本质**：判断**有向图是否存在环**。
2. **三色标记**：DFS 检测环的经典高效方法，区分“未访问”“访问中”“已访问”。
3. **邻接表**：适合存储稀疏图，遍历效率高。
4. **环的判定**：DFS 过程中遇到**状态为 1** 的节点，直接判定有环。
5. **提前终止**：发现环立即返回，无需遍历剩余节点，优化效率。

---

这篇笔记逻辑清晰、注释详细，适配 GitHub 算法笔记归档，面试复习、刷题总结都很实用~
