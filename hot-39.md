# LeetCode 200. 岛屿数量 题解笔记
## 题目描述
给你一个由 `'1'`（陆地）和 `'0'`（水）组成的二维网格，请你计算网格中**岛屿的数量**。

岛屿的定义：
- 陆地被水包围，且通过**上下左右**四个方向相连
- 假设网格的四个边缘都被水包围

## 解题思路
核心解法：**深度优先搜索（DFS）**
1. 遍历二维网格的每一个单元格
2. 当遇到值为 `'1'` 的陆地时，说明找到一个新岛屿，岛屿数量 +1
3. 立即通过 DFS 把**当前岛屿所有相连的陆地**全部标记为已访问（避免重复计数）
4. 遍历完成后，最终的计数就是岛屿总数

## 完整代码（Java）
```java
class Solution {
    public int numIslands(char[][] grid) {
        // 记录岛屿数量
        int ans = 0;
        // 遍历网格每一行
        for (int i = 0; i < grid.length; i++) {
            // 遍历当前行每一列
            for (int j = 0; j < grid[i].length; j++) {
                // 找到未访问的陆地
                if (grid[i][j] == '1') {
                    // DFS 标记整个岛屿
                    dfs(grid, i, j);
                    // 岛屿数量 +1
                    ans++;
                }
            }
        }
        return ans;
    }

    /**
     * DFS 深度优先搜索：标记所有相连的陆地为已访问
     * @param grid 二维网格
     * @param i 当前行坐标
     * @param j 当前列坐标
     */
    private void dfs(char[][] grid, int i, int j) {
        // 递归终止条件：
        // 1. 坐标越界  2. 当前位置不是未访问的陆地
        if (i < 0 || i >= grid.length || j < 0 || j >= grid[0].length || grid[i][j] != '1') {
            return;
        }

        // 标记为已访问（用 '2' 代替，避免重复遍历）
        grid[i][j] = '2';

        // 向 左、右、上、下 四个方向继续搜索
        dfs(grid, i, j - 1); // 左
        dfs(grid, i, j + 1); // 右
        dfs(grid, i - 1, j); // 上
        dfs(grid, i + 1, j); // 下
    }
}
```

## 代码解析
### 1. 主函数 `numIslands`
- **变量 `ans`**：统计最终岛屿数量
- **双层循环**：遍历网格中**每一个单元格**
- **判断条件**：`grid[i][j] == '1'` 表示找到**未被访问的新岛屿**
- 调用 `dfs` 标记整个岛屿，同时 `ans++` 计数

### 2. 辅助函数 `dfs`（深度优先搜索）
#### 递归终止条件
满足以下任一条件直接返回：
- 行/列坐标**越界**（超出网格范围）
- 当前单元格**不是未访问的陆地**（`!= '1'`）

#### 核心逻辑
- **标记已访问**：将当前陆地 `'1'` 改为 `'2'`，避免重复遍历和重复计数
- **四向搜索**：递归遍历**左、右、上、下**四个方向的相邻单元格，覆盖整个岛屿

## 复杂度分析
- **时间复杂度**：$O(M \times N)$
  - $M$ = 网格行数，$N$ = 网格列数
  - 每个单元格最多被访问一次（标记后不再处理）
- **空间复杂度**：$O(M \times N)$
  - 最坏情况（全为陆地），递归深度达到网格总元素数

## 关键点总结
1. **DFS 核心**：遇到陆地就把**整个连通区域**一次性标记完
2. **原地修改**：直接修改网格值标记访问，无需额外开辟空间
3. **四方向遍历**：上下左右才是有效相连，对角线不算
4. **计数时机**：**只有第一次遇到未访问的陆地时才计数**，保证每个岛屿只算一次

---

你可以直接将这段笔记复制到 GitHub 仓库中，格式规范、可读性强，适合作为算法题解归档~
