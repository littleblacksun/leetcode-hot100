<img width="801" height="996" alt="image" src="https://github.com/user-attachments/assets/37030121-4e8e-42b8-81a6-955e3d51cf08" />


# LeetCode 230 二叉搜索树中第K小的元素（中序遍历解法）笔记
这是**利用二叉搜索树中序遍历性质**的最优解法：BST 中序遍历结果是**严格递增序列**，遍历到第 `k` 个节点就是第 `k` 小的元素。

## 题目描述
给定一个二叉搜索树的根节点 `root` 和一个整数 `k`，返回树中**第 k 小**的元素（从 1 开始计数）。

## 核心原理
1. 二叉搜索树 **中序遍历（左→根→右）** 结果 = **严格递增数组**
2. 遍历过程中**计数递减**，当 `k == 0` 时，当前节点就是第 k 小元素
3. 提前返回结果，无需遍历完整棵树，效率更高

---

## 修正后完整可运行代码
你提供的代码存在**返回值类型不匹配**问题，已修正为 LeetCode 标准提交代码：
```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    // 全局变量：记录剩余需要找的第k小
    private int k;

    public int kthSmallest(TreeNode root, int k) {
        this.k = k;
        // 启动中序递归遍历
        return dfs(root);
    }

    /**
     * 中序递归遍历：找到目标节点立即返回
     * @param node 当前节点
     * @return 第k小的节点值
     */
    private int dfs(TreeNode node) {
        // 递归出口：空节点返回 -1（无意义值）
        if (node == null) {
            return -1;
        }

        // 1. 优先递归遍历左子树（中序：左）
        int left = dfs(node.left);
        // 左子树找到结果，直接向上返回
        if (left != -1) {
            return left;
        }

        // 2. 访问当前节点（中序：根）
        // 每访问一个节点，k 减 1
        if (--k == 0) {
            // 找到第 k 小元素，直接返回
            return node.val;
        }

        // 3. 递归遍历右子树（中序：右）
        return dfs(node.right);
    }
}
```

---

## 代码逐行解析
### 1. 全局变量 `k`
- 作用：**剩余需要计数的节点数**
- 为什么用全局变量：递归过程中共享同一个 `k`，保证计数连续

### 2. 主方法 `kthSmallest`
- 初始化全局 `k`
- 调用递归 `dfs` 并返回结果

### 3. 递归方法 `dfs`（核心中序逻辑）
1. **递归左子树**
   严格遵循中序遍历，优先遍历左子树
   若左子树返回有效值（非 -1），说明已找到答案，直接返回

2. **处理当前节点**
   - `--k`：每访问一个节点，剩余计数减 1
   - `k == 0`：**当前节点就是第 k 小元素**，立即返回值

3. **递归右子树**
   左子树和当前节点都没找到，继续遍历右子树

### 4. 终止条件
- 空节点：返回 `-1`（表示未找到目标）
- 找到第 k 小节点：直接返回节点值，**提前终止递归**

---

## 关键细节
1. **提前剪枝**
   找到结果后立即返回，无需遍历完整树，时间效率最优
2. **中序遍历顺序**
   必须严格：**左子树 → 当前节点 → 右子树**
3. **计数逻辑**
   `--k` 先减再判断，保证从 1 开始的计数规则
4. **返回值设计**
   `-1` 表示空节点/未找到，节点值表示找到答案

---

## 复杂度分析
- **时间复杂度**：$O(h + k)$
  - 最好：$O(k)$（左斜树，直接遍历前 k 个节点）
  - 最坏：$O(n)$（链状树）
- **空间复杂度**：$O(h)$
  - 递归调用栈深度，$h$ 为树高度
  - 平衡树：$O(log n)$
  - 链状树：$O(n)$

---

## 思路总结
1. **核心**：BST 中序遍历 = 递增序列
2. **技巧**：全局变量计数 + 递归提前返回
3. **优点**：代码简洁、效率高、无需额外存储遍历序列

---

### 最终结论
这是二叉搜索树第 k 小元素的**最优常规解法**，充分利用 BST 特性，面试高频写法。
