# LeetCode 994. 腐烂的橘子 题解笔记
## 题目描述
在给定的 `m x n` 网格 `grid` 中，每个单元格可以有以下三个值之一：
- 值 `0` 代表空单元格
- 值 `1` 代表新鲜橘子
- 值 `2` 代表腐烂的橘子

每分钟，**腐烂的橘子**会向它**上下左右四个方向**相邻的新鲜橘子腐烂。

请你计算直到单元格中没有新鲜橘子为止所必须经过的最小分钟数。如果不可能腐烂所有橘子，返回 `-1`。

## 解题思路
核心解法：**广度优先搜索（BFS）** —— **多源BFS**
1. **初始化**：遍历网格，统计新鲜橘子数量，同时将**所有初始腐烂橘子**加入队列（多源起点）
2. **层序遍历**：按分钟分层扩散腐烂，每一层代表一分钟，逐层处理所有当前腐烂橘子
3. **扩散感染**：每个腐烂橘子向四邻域扩散，将新鲜橘子变为腐烂，更新队列
4. **结果判断**：遍历结束后，若仍有新鲜橘子剩余返回 `-1`，否则返回耗时分钟数

## 完整代码（Java）
```java
class Solution {
    // 上下左右四个方向数组，简化四邻域遍历代码
    private static final int[][] DIRECTIONS={{-1,0},{1,0},{0,-1},{0,1}};

    public int orangesRotting(int[][] grid) {
        int m = grid.length;    // 网格行数
        int n = grid[0].length; // 网格列数
        int fresh = 0;          // 统计新鲜橘子数量
        List<int[]> q = new ArrayList<>(); // 存储腐烂橘子坐标（BFS队列）

        // 第一步：遍历网格，初始化新鲜橘子数和腐烂橘子队列
        for(int i = 0;i<m;i++){
            for(int j = 0; j<n;j++){
                if(grid[i][j] == 1){
                    fresh++; // 统计新鲜橘子
                }else if(grid[i][j]==2){
                    q.add(new int[]{i,j}); // 初始腐烂橘子入队
                }
            }
        }    
        
        int ans = 0; // 记录腐烂耗时（分钟）
        // BFS循环：有新鲜橘子 且 队列不为空
        while(fresh>0 && !q.isEmpty()){
            ans++;// 每遍历一层，时间 +1 分钟
            
            List<int[]> tmp = q; // 临时存储当前层所有腐烂橘子
            q = new ArrayList<>();// 清空队列，存储下一层新腐烂橘子
            
            // 遍历当前层所有腐烂橘子，向四方向扩散
            for(int[] pos:tmp){
                for(int[] d : DIRECTIONS){
                    // 计算相邻坐标
                    int i = pos[0] + d[0];
                    int j = pos[1] + d[1];
                    // 坐标合法 + 是新鲜橘子
                    if(0<=i && i<m && 0<=j&&j<n&&grid[i][j]==1){
                        fresh--; // 新鲜橘子-1
                        grid[i][j]=2; // 标记为腐烂
                        q.add(new int[]{i,j}); // 加入下一层队列
                    }
                }
            }
        }
        // 剩余新鲜橘子>0：无法完全腐烂，返回-1；否则返回耗时
        return fresh>0 ? -1 : ans;
    }
}
```

## 代码解析
### 1. 全局常量：方向数组
`private static final int[][] DIRECTIONS={{-1,0},{1,0},{0,-1},{0,1}};`
- 简化上下左右四方向遍历，避免重复写4遍坐标计算，代码更简洁优雅

### 2. 初始化遍历
- 统计**新鲜橘子总数**：用于最后判断是否完全腐烂
- 收集**所有初始腐烂橘子**：多源BFS核心，同时从所有腐烂点开始扩散

### 3. BFS 核心循环（分层扩散）
- **分层逻辑**：`tmp` 存储当前分钟所有腐烂橘子，处理完一层时间 `ans+1`
- **扩散逻辑**：遍历每个腐烂橘子的四邻域，将合法新鲜橘子腐烂
- **队列更新**：新腐烂的橘子加入队列，作为下一分钟的扩散源

### 4. 结果返回
- `fresh > 0`：有橘子无法被感染（被隔离），返回 `-1`
- 否则：返回总耗时 `ans`

## 复杂度分析
- **时间复杂度**：$O(M \times N)$
  - $M$ = 行数，$N$ = 列数
  - 每个单元格最多入队、出队一次，遍历次数为网格总单元格数
- **空间复杂度**：$O(M \times N)$
  - 最坏情况：网格全是腐烂橘子，队列存储所有橘子坐标

## 关键点总结
1. **多源BFS**：同时从多个初始腐烂橘子开始扩散，保证最小时间
2. **分层遍历**：一层对应一分钟，精准计时
3. **新鲜橘子计数**：提前统计，快速判断最终结果
4. **方向数组**：简化四方向遍历，代码更整洁
5. **终止条件**：无新鲜橘子 或 无腐烂橘子可扩散

---

这篇笔记格式规范、逻辑清晰，直接复制即可用于 GitHub 算法归档，适配面试总结、刷题笔记场景~
