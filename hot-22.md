<img width="912" height="902" alt="image" src="https://github.com/user-attachments/assets/87c0a627-3333-4c47-bec0-34aa6dfccea5" />


```java
/**
 * Definition for singly-linked list.
 * 单向链表节点定义，LeetCode 题目自带
 */
public class ListNode {
    int val;        // 节点存储的值
    ListNode next;  // 指向下一个节点的引用
    ListNode() {}   // 无参构造方法
    ListNode(int val) { this.val = val; } // 带节点值的构造方法
    ListNode(int val, ListNode next) { this.val = val; this.next = next; } // 带值+下一节点的构造
}

/**
 * 主解答类
 */
class Solution {

    /**
     * 对外公开的方法：两数相加
     * @param l1 第一个链表头节点
     * @param l2 第二个链表头节点
     * @return 相加后新链表的头节点
     */
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        // 调用递归辅助方法，初始进位 carry = 0
        return addTwo(l1, l2, 0);
    }

    /**
     * 递归核心方法：逐位相加 + 处理进位
     * @param l1 当前遍历到的 l1 节点
     * @param l2 当前遍历到的 l2 节点
     * @param carry 上一位计算产生的进位
     * @return 当前位构造的新节点
     */
    private ListNode addTwo(ListNode l1, ListNode l2, int carry) {
        // 递归边界条件：两个链表都遍历完，且没有进位 → 递归结束
        if (l1 == null && l2 == null && carry == 0) {
            return null;
        }

        // 初始化当前位的和 → 先把进位加进来
        int sum = carry;

        // 如果 l1 当前节点不为空 → 把值加到 sum 中，并将 l1 后移
        if (l1 != null) {
            sum += l1.val;
            l1 = l1.next;
        }

        // 如果 l2 当前节点不为空 → 把值加到 sum 中，并将 l2 后移
        if (l2 != null) {
            sum += l2.val;
            l2 = l2.next;
        }

        // 构造当前节点：值 = sum % 10（当前位）
        // 递归构造下一节点：传入下一轮的 l1、l2，以及新的进位 sum / 10
        return new ListNode(sum % 10, addTwo(l1, l2, sum / 10));
    }
}
```

---

# GitHub Markdown 格式：解题思路 & 解答方法

# LeetCode 2. 两数相加（递归解法）

## 题目描述
给出两个**非空**的链表，表示两个非负的整数。它们每位数字都是按照**逆序**的方式存储的，并且每个节点只能存储**一位**数字。

请你将两个数相加，并以相同形式返回一个表示和的链表。

## 解题思路（递归）
这道题最直观的思路是**模拟人工加法**：
1. 两个链表都是**逆序存储**，直接从左到右遍历就是从**个位到高位**，非常适合直接相加。
2. 每一位相加 = 链表1当前位 + 链表2当前位 + 上一位的进位。
3. 当前位结果 = 和对 10 取余。
4. 新的进位 = 和除以 10。
5. 当两个链表都遍历完毕，且进位为 0 时，结束计算。

**递归思路核心：**
- 把**每一位的计算**看作一个子问题。
- 每次只处理**当前位**，然后递归处理**下一位**。
- 递归边界：两个链表都为空 + 无进位。

## 解法步骤
1. **边界判断**：如果两个链表都遍历完，且没有进位，返回 `null`。
2. **计算当前位总和**：进位 + l1当前值（若存在）+ l2当前值（若存在）。
3. **移动指针**：l1、l2 分别后移。
4. **构造当前节点**：值为 `sum % 10`。
5. **递归构造下一节点**：传入新的 l1、l2 和新进位 `sum / 10`。

## 代码关键点解释
- **递归函数参数**：`l1、l2`（当前节点）+ `carry`（进位）。
- **sum%10**：得到当前位应该存储的值。
- **sum/10**：得到传递到下一位的进位。
- **天然处理不等长链表**：节点为空时，视为 0 参与计算。
- **天然处理最高位进位**：递归会自动多生成一个节点存储进位。

## 复杂度分析
- **时间复杂度**：$O(\max(m, n))$
  - m、n 分别为两个链表的长度，只遍历最长的链表一次。
- **空间复杂度**：$O(\max(m, n))$
  - 递归调用栈深度等于链表最长长度。

---

### 总结
1. 这是**纯递归实现**的两数相加，代码简洁优雅；
2. 逐位求和 + 处理进位，完全模拟手工加法；
3. 自动处理**不等长链表**和**最高位进位**；
4. 递归边界清晰，逻辑直观，非常适合理解递归思想。

我为你**逐行详细注释**这段迭代法代码，然后用标准 **GitHub Markdown** 格式写完整思路和解答，和上一版格式完全统一。

# 完整带逐行注释代码
```java
/**
 * Definition for singly-linked list.
 * 单向链表节点定义，LeetCode 题目自带
 */
public class ListNode {
    int val;        // 节点存储的数字
    ListNode next;  // 指向下一个节点的指针
    ListNode() {}
    ListNode(int val) { this.val = val; }
    ListNode(int val, ListNode next) { this.val = val; this.next = next; }
}

class Solution {
    /**
     * 迭代法（非递归）两数相加
     * @param l1 第一个数字链表（逆序）
     * @param l2 第二个数字链表（逆序）
     * @return 相加后的结果链表头节点
     */
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        // 创建哨兵（虚拟头）节点，作用：简化链表头节点的创建逻辑
        ListNode dummy = new ListNode(); 
        // cur 指针：指向当前正在构建的结果链表的最后一个节点
        ListNode cur = dummy;
        // 进位变量：存储两数相加后产生的进位，初始为 0
        int carry = 0;

        // 循环条件：
        // l1没遍历完 OR l2没遍历完 OR 还有进位未处理 → 必须继续循环
        while (l1 != null || l2 != null || carry != 0) {
            // 如果 l1 当前节点不为空，把值加到 carry 里
            if (l1 != null) {
                carry += l1.val;  // carry = 进位 + l1当前位值
                l1 = l1.next;     // l1 指针后移
            }
            // 如果 l2 当前节点不为空，把值加到 carry 里
            if (l2 != null) {
                carry += l2.val;  // carry = 进位 + l1当前位 + l2当前位
                l2 = l2.next;     // l2 指针后移
            }

            // 1. 创建新节点：值 = carry % 10（当前位结果）
            // 2. 把新节点挂到 cur.next
            // 3. cur 指针移动到新节点（更新为最后一个节点）
            cur = cur.next = new ListNode(carry % 10);
            
            // 更新进位：carry = carry / 10（只保留十位及以上）
            carry /= 10;
        }

        // 哨兵节点的 next 才是真正的结果链表头
        return dummy.next;
    }
}
```

---


# LeetCode 2. 两数相加（迭代法）

## 题目描述
给出两个**非空**的链表，表示两个非负整数。  
它们每位数字按照**逆序**方式存储，每个节点只存储**一位数字**。  
将两数相加，以相同形式返回表示和的链表。

## 解题思路（迭代法）
迭代法就是**模拟手工竖式加法**，一步一步从左到右（个位到高位）相加：

1. **逆序天然适配**：链表从左到右 = 数字从低位到高位，直接相加即可。
2. **逐位相加**：当前位 = l1当前位 + l2当前位 + 进位。
3. **保存当前位**：和对 10 取余，存入新节点。
4. **更新进位**：和除以 10，留给下一位使用。
5. **循环结束条件**：两个链表都遍历完，且没有进位。

**迭代法核心特点**：
- 用**循环**代替递归，更节省栈空间。
- 使用**哨兵节点（dummy）** 简化链表头节点创建。
- 一个循环搞定所有情况：不等长链表、最高位进位。

## 解法步骤
1. **创建哨兵节点**：统一头节点创建逻辑，避免空指针判断。
2. **初始化指针与进位**：`cur` 指向结果链表尾部，`carry = 0`。
3. **循环遍历**：只要链表没走完或还有进位，就继续计算。
4. **累加求和**：把 l1、l2 当前值都加到 `carry` 中。
5. **创建新节点**：值为 `carry % 10`，并连接到结果链表。
6. **更新进位**：`carry = carry / 10`。
7. **移动指针**：l1、l2、cur 都向后移动。
8. **返回结果**：哨兵节点的 next 即为结果头节点。

## 代码关键点解释
- **哨兵节点 dummy**  
  虚拟头节点，不用单独处理结果链表第一个节点的特殊情况，代码更简洁。
- **carry 复用**  
  既存储进位，又存储当前位总和，简化变量。
- **循环条件 `l1!=null || l2!=null || carry!=0`**  
  覆盖所有边界：
  - 链表一长一短
  - 最高位相加后仍有进位（如 999 + 999 = 1998）
- **cur = cur.next = new ListNode(...)**  
  一行代码完成：创建节点 → 连接节点 → 移动指针。

## 复杂度分析
- **时间复杂度**：$O(\max(m, n))$  
  m、n 为两个链表长度，只遍历最长的链表一次。
- **空间复杂度**：$O(\max(m, n))$  
  只用于存储结果链表，**无额外递归栈空间**。

---

### 总结
1. 迭代法是**更推荐的工业级写法**，无栈溢出风险。
2. 利用**哨兵节点**让代码简洁优雅。
3. 一个循环处理**不等长链表**和**最后进位**，逻辑清晰。
4. 逐位模拟加法，直观、易理解、易编写、易调试。

