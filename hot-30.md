<img width="742" height="804" alt="image" src="https://github.com/user-attachments/assets/09bd5f12-7189-40de-b5d3-32f64d33bbb8" />


<img width="1256" height="973" alt="image" src="https://github.com/user-attachments/assets/79a975a5-5bc0-443a-8eb8-551085f4022d" />

# LeetCode 543. 二叉树的直径 题解笔记
## 题目描述
给定一棵二叉树，你需要计算它的直径长度。一棵二叉树的直径长度是任意两个结点路径长度中的最大值。这条路径可能穿过也可能不穿过根结点。

**注意**：两结点之间的路径长度是以它们之间边的数目表示。

## 解题思路
核心思路：**二叉树的直径 = 某节点的左子树最大深度 + 右子树最大深度**，我们需要遍历所有节点，计算每个节点的左右子树深度和，取最大值即为答案。

1. 采用深度优先搜索（DFS）遍历二叉树，递归计算每个节点的子树深度；
2. 用全局变量维护遍历过程中遇到的最大直径（左深度+右深度）；
3. 递归函数返回当前节点的最大子树深度，供父节点计算直径使用。

## 完整代码（Java）
```java
class Solution {
    // 全局变量，记录二叉树的最大直径
    private int ans;

    public int diameterOfBinaryTree(TreeNode root) {
        ans = 0; // 初始化最大值
        dfs(root);
        return ans;
    }

    /**
     * 深度优先搜索：计算以当前节点为根的子树的最大深度
     * @param node 当前遍历的节点
     * @return 当前节点的最大子树深度
     */
    private int dfs(TreeNode node){
        // 递归终止条件：空节点深度为 -1（抵消后续 +1 操作，让叶子节点子树深度为 0）
        if(node == null){
            return -1;
        }
        // 左子树深度 = 左孩子递归结果 + 1（当前节点到左孩子的一条边）
        int lLen = dfs(node.left) + 1;
        // 右子树深度 = 右孩子递归结果 + 1（当前节点到右孩子的一条边）
        int rLen = dfs(node.right) + 1;
        
        // 更新最大直径：当前节点的直径 = 左深度 + 右深度
        ans = Math.max(ans, lLen + rLen);
        
        // 返回当前节点的最大子树深度（供父节点计算使用）
        return Math.max(lLen, rLen);
    }
}
```

## 代码细节解析
### 1. 关键变量说明
- `private int ans`：全局私有变量，**实时记录遍历过程中最大的直径值**，最终直接返回该变量即可。

### 2. 递归终止条件（空节点返回 -1）
```java
if(node == null){
    return -1;
}
```
- 叶子节点的左右孩子都是空节点，返回 `-1` 后，`+1` 刚好得到叶子节点的子树深度为 `0`，符合「路径长度=边数」的题目要求；
- 若返回 `0`，会导致深度计算多算一层，最终结果错误。

### 3. 核心计算逻辑
```java
// 左、右子树深度
int lLen = dfs(node.left) + 1;
int rLen = dfs(node.right) + 1;

// 更新最大直径
ans = Math.max(ans, lLen + rLen);

// 返回当前节点最大子树深度
return Math.max(lLen, rLen);
```
1. 递归获取左右子树的深度，分别加 1（代表当前节点到子节点的边）；
2. 用当前节点的「左深度+右深度」更新全局最大直径；
3. 返回当前节点的最大子树深度，用于父节点继续计算直径。

## 复杂度分析
- **时间复杂度**：$O(n)$，n 为二叉树节点数，每个节点仅遍历一次；
- **空间复杂度**：$O(h)$，h 为二叉树高度，递归调用栈的深度取决于树的高度，最坏情况（链状树）为 $O(n)$。

## 示例验证
输入：`root = [1,2,3,4,5]`
二叉树结构：
```
    1
   / \
  2   3
 / \
4   5
```
- 节点 2 的左深度=1，右深度=1，和为 2；
- 节点 1 的左深度=2，右深度=1，和为 3；
- 最终最大直径为 `3`（路径：4→2→5→1→3 或 4→2→1→3），与代码输出一致。

---

### 总结
1. 本题核心是**直径 = 节点左子树深度 + 右子树深度**，需遍历所有节点取最大值；
2. 递归返回子树深度，全局变量维护最大直径，是后序遍历的经典应用；
3. 空节点返回 `-1` 是适配题目「边数计数」的关键细节。
